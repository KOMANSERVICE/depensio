name: 🚀 Deploy on Main PR Merge

on:
  workflow_dispatch:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  deploy:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v3

      - name: 🚀 Deploy to Production Server via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          port: 22
          script: |
            echo "Suppression de dossier exitant"
            sudo rm -rf depensio
            echo "➡️ Récupérer le pojet sur github"
            git clone https://laserda:${{ secrets.TOKEN }}@github.com/KOMANSERVICE/depensio.git
            echo "➡️ Copier le fichier setup.deploy.sh"
            cp /home/komanatse/vaultdepensio/setup.deploy.sh /home/komanatse/depensio/vault/policies
            chmod +x /home/komanatse/depensio/vault/policies/setup.deploy.sh
            echo "➡️ Créer le dossier shared"
            mkdir -p /home/komanatse/depensio/vault/shared
            echo "➡️ Passage au dossier projet"
            cd /home/komanatse/depensio
            echo "📦 Pull du nouveau contenu"
            git pull origin main
            echo "♻️ Rebuild & redeploy"
            docker compose -f docker-compose.prod.yml down
            docker compose -f docker-compose.prod.yml up -d --build
