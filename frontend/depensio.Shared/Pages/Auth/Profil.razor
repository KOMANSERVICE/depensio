@page "/myprofile"
@inject NavigationManager _navigationManager
@layout SecondaryLayout
@inject IAuthUserService _authUserService
@inject ToastService ToastService


<LoadingComponent IsLoad="@isLoad" />
<ToastContainer />
<PageTitleComponent Title="Mon profil" />

<div class="container mx-auto px-4 py-8 max-w-4xl">
    <!-- Header -->
    <header class="flex items-center justify-between mb-8">
        <div class="flex items-center">
            <h1 class="text-3xl font-bold text-primary-700 text-gray-900 sm:text-2xl dark:text-white">Mon Profil</h1>
        </div>
        <div class="flex items-center space-x-2">
            <span class="text-sm text-gray-500">Dernière mise à jour: Aujourd'hui</span>
        </div>
    </header>

    <!-- Profile Picture Section -->
    @* <div class="flex flex-col items-center mb-10">
        <div class="relative profile-pic-upload group">
            <img src="http://static.photos/people/200x200/42" alt="Photo de profil"
                 class="w-32 h-32 rounded-full object-cover border-4 border-white shadow-lg">
            <div class="absolute inset-0 bg-black bg-opacity-30 rounded-full flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity duration-200 cursor-pointer">
                <div class="upload-icon bg-white bg-opacity-80 p-3 rounded-full">
                    <i data-feather="camera" class="text-gray-700"></i>
                </div>
            </div>
        </div>
        <button class="mt-4 text-sm text-primary-700 font-medium hover:text-[#a3722c] transition-colors duration-200">
            Changer la photo
        </button>
    </div> *@
    
    <EditForm EditContext="@editContext" OnValidSubmit="HandleUpdate">
        <DataAnnotationsValidator />
        <ValidationSummaryFancy />
        <!-- Profile Form Sections -->
        <div class="space-y-6">
            <!-- Personal Information Section -->
            <div class="bg-white rounded-xl shadow-sm p-6 section-card">
                <div class="flex items-center justify-between mb-4">
                    <h2 class="text-lg font-semibold text-gray-800">
                        <i data-feather="user" class="inline mr-2 text-primary-700"></i>
                        Informations personnelles @user.Email
                    </h2>
                    <span class="text-xs text-gray-500">Requis</span>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <label for="firstName" class="block text-sm font-medium text-gray-700 mb-1">Prénom</label>
                        <InputText @bind-Value="user.FirstName" id="firstName"
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg input-focus focus:outline-none transition-colors duration-200"
                               />
                    </div>
                    <div>
                        <label for="lastName" class="block text-sm font-medium text-gray-700 mb-1">Nom</label>
                        <InputText @bind-Value="user.LastName"  id="lastName"
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg input-focus focus:outline-none transition-colors duration-200"
                               />
                    </div>
                  @*   <div class="md:col-span-2">
                        <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                        <InputText @bind-Value="user.Email" type="email" id="email"
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg input-focus focus:outline-none transition-colors duration-200"
                               />
                    </div> *@
                </div>
            </div>

            <!-- Contact Information Section -->
            <div class="bg-white rounded-xl shadow-sm p-6 section-card">
                <div class="flex items-center justify-between mb-4">
                    <h2 class="text-lg font-semibold text-gray-800">
                        <i data-feather="phone" class="inline mr-2 text-primary-700"></i>
                        Coordonnées
                    </h2>
                    <span class="text-xs text-gray-500">Optionnel</span>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <label for="phone" class="block text-sm font-medium text-gray-700 mb-1">Téléphone</label>
                        <InputText @bind-Value="user.Tel" id="phone"
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg input-focus focus:outline-none transition-colors duration-200"
                               />
                    </div>
                   @*  <div class="md:col-span-2">
                        <label for="address" class="block text-sm font-medium text-gray-700 mb-1">Adresse</label>
                        <input type="text" id="address"
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg input-focus focus:outline-none transition-colors duration-200"
                               value="123 Rue de la République, Paris">
                    </div> *@
                </div>
            </div>

            <!-- Preferences Section -->
            <div class="bg-white rounded-xl shadow-sm p-6 section-card">
                <div class="flex items-center justify-between mb-4">
                    <h2 class="text-lg font-semibold text-gray-800">
                        <i data-feather="settings" class="inline mr-2 text-primary-700"></i>
                        Préférences
                    </h2>
                    <span class="text-xs text-gray-500">Personnalisation</span>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <label for="language" class="block text-sm font-medium text-gray-700 mb-1">Langue</label>
                        <select id="language"
                                class="w-full px-4 py-2 border border-gray-300 rounded-lg input-focus focus:outline-none transition-colors duration-200">
                            <option value="fr">Français</option>
                          @*   <option value="en">English</option>
                            <option value="es">Español</option> *@
                        </select>
                    </div>
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Notifications</label>
                        <div class="flex items-center space-x-4">
                            <label class="inline-flex items-center">
                                <input type="checkbox" class="rounded text-primary-700 focus:ring-[#825513]" checked>
                                <span class="ml-2 text-sm text-gray-700">Email</span>
                            </label>
                           @*  <label class="inline-flex items-center">
                                <input type="checkbox" class="rounded text-primary-700 focus:ring-[#825513]">
                                <span class="ml-2 text-sm text-gray-700">SMS</span>
                            </label> *@
                        </div>
                    </div>
                </div>
            </div>
    
        </div>

        <!-- Action Buttons -->
        <div class="mt-10 flex flex-col sm:flex-row justify-end space-y-3 sm:space-y-0 sm:space-x-4">
          
            <button type="submit" class="mt-4 md:mt-0 bg-primary-700 hover:bg-amber-800 text-white px-6 py-2 rounded-lg flex items-center space-x-2 transition">
                Enregistrer les modifications
            </button>
        </div>

    </EditForm>

</div>

@code {

    private bool isLoad = false;
    private UserInfosDTO user = new();

    private ErrorMessage errorMessage = new();

    private EditContext? editContext;
    private ValidationMessageStore? messageStore;

    protected override async Task OnInitializedAsync()
    {
        editContext = new EditContext(user);
        messageStore = new ValidationMessageStore(editContext);

        editContext.OnValidationRequested += (s, e) => messageStore.Clear();
        editContext.OnFieldChanged += (s, e) => messageStore.Clear(e.FieldIdentifier);

        await GetUserInfosAsync();
    }

    private async Task GetUserInfosAsync()
    {
        var result = await _authUserService.GetUserInfosAsync();
        if (result.Success)
        {
            user = result.Data.UserInfos;
        }
    }

    private async Task HandleUpdate()
    {
        isLoad = true;
        try
        {
            var result = await _authUserService.UpdateUserAsync(new UpdateUserRequest(user));

            if (result.Success)
            {
                ShowSuccess();
                messageStore?.Clear();
                editContext?.NotifyValidationStateChanged();
                await GetUserInfosAsync();
            }

        }
        catch (ApiException ex)
        {
            ShowError();
            try
            {
                var errorResult = JsonSerializer.Deserialize<ErrorMessage>(ex.Content, new JsonSerializerOptions
                {
                    PropertyNameCaseInsensitive = true
                });


                // Efface les anciennes erreurs
                messageStore.Clear();

                if (errorResult?.ValidationErrors != null && errorResult.ValidationErrors.Any())
                {
                    foreach (var validationError in errorResult.ValidationErrors)
                    {
                        // Nettoyer le nom de propriété (ex: Product.Name -> Name)
                        var propertyName = validationError.PropertyName;
                        if (propertyName.Contains('.'))
                            propertyName = propertyName.Split('.').Last();

                        // Associer au champ si possible
                        var fieldIdentifier = new FieldIdentifier(user!, propertyName);
                        messageStore.Add(fieldIdentifier, validationError.ErrorMessage);
                    }
                }
                else if (!string.IsNullOrWhiteSpace(errorResult?.Detail))
                {
                    // Message global sans champ spécifique
                    var fieldIdentifier = new FieldIdentifier(user!, string.Empty);
                    messageStore.Add(fieldIdentifier, errorResult.Detail);
                }

                // Rafraîchir l’affichage
                editContext.NotifyValidationStateChanged();


            }
            catch
            {
                messageStore?.Add(new FieldIdentifier(user, string.Empty), "Une erreur est survenue.");
                editContext?.NotifyValidationStateChanged();
            }
        }
        finally
        {
            isLoad = false;
        }
    }

    private void ShowSuccess()
    {
        ToastService.ShowToast("Succès", "Enregistré avec succès", "success");
    }

    private void ShowError()
    {
        ToastService.ShowToast("Erreur", "Une erreur est survenue", "error");
    }
}